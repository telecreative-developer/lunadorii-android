import React, { Component } from 'react'
import { View, Text } from 'react-native'

const NoDefaultShippingAddress = (props) => (
  <View style={{alignItems: 'center'}}>
    <Text style={{textAlign: 'center'}}>No default shipping address</Text>
    <Text style={{textAlign: 'center'}}>add now</Text>
  </View>
)

export default NoDefaultShippingAddress